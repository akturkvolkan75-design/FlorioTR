import sharp from "sharp";
import {NextResponse} from "next/server";
import {prisma} from "@/lib/prisma";
import {savePublicImage} from "@/lib/image-storage";

const allowed=["image/jpeg","image/png","image/webp","image/avif"];
export async function POST(request:Request){try{const form=await request.formData();const orderId=Number(form.get("orderId"));const flowerShopId=Number(form.get("flowerShopId"));const file=form.get("image");if(!orderId||!flowerShopId||!(file instanceof File)||!file.size)return NextResponse.json({success:false,message:"Sipariş ve fotoğraf bilgisi eksik."},{status:400});if(file.size>10*1024*1024||!allowed.includes(file.type))return NextResponse.json({success:false,message:"Fotoğraf JPG, PNG, WebP veya AVIF ve en fazla 10 MB olmalıdır."},{status:400});const order=await prisma.order.findFirst({where:{id:orderId,flowerShopId},include:{customer:{select:{email:true,name:true}}}});if(!order)return NextResponse.json({success:false,message:"Bu sipariş size ait değil."},{status:403});const optimized=await sharp(Buffer.from(await file.arrayBuffer())).rotate().resize({width:1400,height:1400,fit:"inside",withoutEnlargement:true}).webp({quality:84}).toBuffer();const preparationImage=await savePublicImage({bucket:"order-preparations",prefix:`order-${orderId}`,contents:optimized});await prisma.order.update({where:{id:orderId},data:{preparationImage}});let notificationSent=false;const apiKey=process.env.RESEND_API_KEY;if(apiKey&&order.customer?.email){try{const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({from:process.env.VERIFICATION_EMAIL_FROM||"FlorioTR <onboarding@resend.dev>",to:[order.customer.email],subject:"Çiçeğiniz hazırlandı 🌸",html:`<div style="font-family:Arial;padding:24px;color:#17382f"><h2>Merhaba ${order.customer.name.replace(/[<>]/g,"")},</h2><p><strong>${order.productName.replace(/[<>]/g,"")}</strong> siparişiniz çiçekçiniz tarafından hazırlandı.</p><p>Hazırlanan çiçeğin gerçek fotoğrafını FlorioTR hesabınızdaki <strong>Siparişlerim</strong> bölümünden görebilirsiniz.</p><p style="margin-top:24px">FlorioTR 🌿</p></div>`})});notificationSent=response.ok;if(!response.ok)console.error("Hazırlık bildirimi gönderilemedi:",response.status);}catch(error){console.error("Hazırlık bildirimi gönderilemedi:",error)}}return NextResponse.json({success:true,preparationImage,notificationSent,message:"Hazırlık fotoğrafı müşteriye gönderildi."});}catch(error){console.error("Hazırlık fotoğrafı yüklenemedi:",error);return NextResponse.json({success:false,message:"Fotoğraf yüklenemedi."},{status:500});}}
