"use client";
import {useCallback,useEffect,useState} from "react";
type Review={id:number;orderId:number;productName:string;rating:number;comment:string|null;customerName:string;isApproved:number|boolean;createdAt:string};
export default function AdminReviews(){const [reviews,setReviews]=useState<Review[]>([]);const load=useCallback(()=>fetch("/api/admin/reviews").then(r=>r.json()).then(d=>{if(d.success)setReviews(d.reviews)}),[]);useEffect(()=>{load()},[load]);async function update(id:number,isApproved:boolean){await fetch("/api/admin/reviews",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,isApproved})});load()}async function remove(id:number){if(!confirm("Bu yorumu silmek istediğinize emin misiniz?"))return;await fetch("/api/admin/reviews",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({id})});load()}return <main className="min-h-screen bg-slate-100 p-6 text-slate-950"><div className="mx-auto max-w-6xl"><div className="rounded-3xl bg-[#0c2f27] p-7 text-white"><h1 className="text-3xl font-black">Müşteri Yorumları</h1><p className="mt-2 text-white/80">Doğrulanmış alışveriş yorumlarını onaylayın veya gizleyin.</p></div><div className="mt-6 grid gap-4">{reviews.length===0?<div className="rounded-2xl bg-white p-8 text-center font-bold shadow">Henüz müşteri yorumu yok.</div>:reviews.map(review=><article key={review.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-wider text-amber-700">Sipariş #{review.orderId} · {review.productName}</p><h2 className="mt-2 text-lg font-black">{review.customerName}</h2><p className="mt-1">{"⭐".repeat(review.rating)}</p><p className="mt-3 font-medium text-slate-700">{review.comment||"Yalnızca puan verildi."}</p><p className="mt-3 text-xs font-bold text-slate-500">{new Date(review.createdAt).toLocaleDateString("tr-TR")}</p></div><span className={`rounded-full px-4 py-2 text-sm font-black ${review.isApproved?"bg-emerald-100 text-emerald-900":"bg-amber-100 text-amber-900"}`}>{review.isApproved?"Yayında":"Onay bekliyor"}</span></div><div className="mt-5 flex flex-wrap gap-2"><button onClick={()=>update(review.id,true)} className="rounded-xl bg-emerald-700 px-5 py-2.5 font-black text-white">Onayla</button><button onClick={()=>update(review.id,false)} className="rounded-xl bg-amber-500 px-5 py-2.5 font-black text-slate-950">Gizle</button><button onClick={()=>remove(review.id)} className="rounded-xl bg-red-700 px-5 py-2.5 font-black text-white">Sil</button></div></article>)}</div></div></main>}
